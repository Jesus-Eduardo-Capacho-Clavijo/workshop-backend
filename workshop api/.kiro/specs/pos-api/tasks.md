# Implementation Tasks

1. Setup Spring Boot project with dependencies.
2. Define Entities (`Sale`, `SaleItem`) and Enums (`SaleStatus`, `PaymentType`).
3. Define DTOs for requests and responses.
4. Implement External API Clients with RestTemplate.
5. Implement Repositories.
6. Implement `SaleService` containing business rules and state transitions.
7. Expose REST endpoints in `SaleController`.
8. Implement `GlobalExceptionHandler`.
9. Write unit tests for `SaleService`.
10. Write integration tests and verify JaCoCo coverage.
